cabal-version: 3.0
name: circuits-stats
version: 0.1.0.0
license: BSD-3-Clause
license-file: LICENSE
copyright: Tony Day (c) 2025-2026
category: control
author: Tony Day
maintainer: tonyday567@gmail.com
homepage: https://github.com/tonyday567/circuits-stats#readme
bug-reports: https://github.com/tonyday567/circuits-stats/issues
synopsis: Statistical processes on top of circuits.
description:
  @circuits-stats@ provides streaming statistics and other state-machine
  specialisations built on the 'Circuit.Process' carrier from @circuits@.
  The arrow itself lives in @circuits@; this package is the box library:
  moving averages, standard deviations, quantiles, ODEs, differentiable
  processes, and simulation helpers.

  The 'fold' runner is re-exported from "Circuit.Process" and is total
  (returns 'Nothing' on empty input). The @circuits-stats-axioma@ test suite
  contains regression oracles for the statistics, ODE integrators, quantiles,
  and gradient runners.

  == Usage

  >>> import Circuit.Stats
  >>> import Data.Maybe (fromMaybe)

  >>> fromMaybe (0/0) $ fold ((,) <$> ma 0.9 <*> std 0.9) [1..100]
  (91.00265621044142,9.472822805289121)

build-type: Simple
tested-with:
  ghc ==9.10.3
  ghc ==9.12.2
  ghc ==9.14.1

extra-doc-files:
  ChangeLog.md
  readme.md

source-repository head
  type: git
  location: https://github.com/tonyday567/circuits-stats

common ghc-options-stanza
  ghc-options:
    -Wall
    -Wcompat
    -Widentities
    -Wincomplete-record-updates
    -Wincomplete-uni-patterns
    -Wpartial-fields
    -Wredundant-constraints
    -Wno-pattern-namespace-specifier

library
  import: ghc-options-stanza
  default-language: GHC2024
  hs-source-dirs: src
  build-depends:
    adjunctions >=4.0 && <4.5,
    base >=4.14 && <5,
    circuits >=0.2 && <0.5,
    circuits-diff >=0.1 && <0.2,
    circuits-mat >=0.1 && <0.2,
    containers >=0.6 && <0.9,
    dunning-t-digest >=0.1 && <0.2,
    harpie >=0.1 && <0.4,
    mwc-probability >=2.3.1 && <2.4,
    numhask >=0.14 && <0.15,
    primitive >=0.7.2 && <0.10,
    profunctors >=5.6.2 && <5.7,
    text >=1.2 && <2.2,
    vector >=0.12.3 && <0.14,
    vector-algorithms >=0.8.0 && <0.10,

  exposed-modules:
    Circuit.Stats
    Circuit.Stats.Diff
    Circuit.Stats.ODE
    Circuit.Stats.Quantiles
    Circuit.Stats.Simulate

test-suite circuits-stats-axioma
  import: ghc-options-stanza
  type: exitcode-stdio-1.0
  main-is: Oracle.hs
  hs-source-dirs: test
  default-language: GHC2024
  build-depends:
    base >=4.14 && <5,
    circuits >=0.2 && <0.5,
    circuits-diff >=0.1 && <0.2,
    circuits-stats,
    harpie >=0.1 && <0.4,
    numhask >=0.14 && <0.15,
